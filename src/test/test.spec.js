describe("A test suite", function () {
    'use strict';

    beforeEach(function () {

    });

    afterEach(function () {

    });

    it('that should fail has', function () {
        expect(true).to.be.equal(false);
    });
});
